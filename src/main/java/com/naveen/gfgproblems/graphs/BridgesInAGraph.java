package com.naveen.gfgproblems.graphs;

// Bridges in a graph are the edges removing them makes the graph into more than one connected components
// they are similar to articulation points, but they are edges instead of vertices
// dfs traversal tree
// for edge u -> v, if low[v] > dis[u] then its a bridge
public class BridgesInAGraph {
}
